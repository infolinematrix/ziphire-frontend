import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // images: {
  //   domains: ['via.placeholder.com', 'img.freepik.com', 'images.unsplash.com', 'www.shutterstock.com'],
  // },
};

export default nextConfig;
